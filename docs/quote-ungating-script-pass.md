# 原话去门控内容修订单（quote-ungating pass）

日期：2026-07-04
执笔：Claude（剧本层）；实施：Codex（优先改 `content/packs/steam-demo-01/` 下 JSON 与生成索引；若 `src/caseEngine.js` 仍保留同 plot 兜底模板，必须同步，避免双源漂移）
依据：`project-skills/detective-plot-coupling-review/SKILL.md` 公平性检查「引语出处不得被分支门控」；`project-skills/case-scriptwriting/SKILL.md` 整合写作循环与语言规则。

## 背景

一次承诺制关键追问上线后，每个场景玩家只能听到一条回答。伏笔账本审计（见 `docs/unfinished-backlog.md` 2026-07-04 条目）发现四案共 4 处最终原话的唯一出处在分支回答里，走另一条路的玩家没听过它们。修法统一为：**把原话织进无门控的 `sceneVersions[].version` 正文**，并对因此产生重复的分支回答做去重改写。若本文的旧问法与后续 `case-scriptwriting` 的“真实连线口吻 / 禁止审问腔”规则冲突，以当前 skill 和测试为准；但必须保留无门控出处、去重意图和 `quotePickCandidates` / `accusationChoices` 一致性。

## 全包引语↔出处账本（修订后应全部成立）

| 案 | accusationChoices 标签 | 无门控出处 | 状态 |
|---|---|---|---|
| 01 | 我只是怕你知道我失业后就离开我 | V3 version | 已合规 |
| 01 | 我也怕别人觉得我找了个撑不住场面的人 | V4 version | **本次修** |
| 01 | 账单其实还有三天才到期 | V2 version | 已合规 |
| 01 | 以后他可能就不敢跟我谈结婚了 | V4 version | 已合规 |
| 02 | 只有我能接住 TA 的情绪 | V0 version | 已合规 |
| 02 | 他说我像店里自己人 | V1 version | **本次修** |
| 02 | 我从来没说只有你一个 | V3 version | 已合规 |
| 02 | 以后店开起来，你就是老板娘 | V3 version | 已合规 |
| 03 | 他一直说名校毕业，细问才说是 MBA | V2 version（紧压缩） | 合规，只记账 |
| 03 | 我只说他学校那边确实是真的 | V2 version | **本次修** |
| 03 | 再问下去，是不是工资卡也要交出来？ | V4 version | 已合规 |
| 03 | 结婚以后钱最好放一起管 | V4 version | 已合规 |
| 04 | 报销审批通过 | V2 version | 已合规 |
| 04 | 我也确实想要这个主责 | V0 version | **本次修** |
| 04 | 返款统一打给对接人 | V3 version | 已合规 |
| 04 | 先私下把事办成，复盘再补流程 | V1 version | 已合规 |

此表修订完成后整体粘贴进 `content/packs/steam-demo-01/qa-report.md` 作为引语映射账本。

---

## 案 1（01-credit.json）

走向表（不变部分）：V0 断缴时间 → V1 账单用途 → V2 期限造急 → V3 情绪递账 → V4 结婚绑架+来电人自认。材料板 1 块，回流 1 条，均不动。

### 修改 1：`sceneVersions[4].version` 整段替换

旧：

> 还有一句我没好意思说：他提过如果我这次不帮，以后他可能就不敢跟我谈结婚了，说自己会一直觉得低我一头。我前面也没说全，那周我们刚吃过很贵的纪念日晚餐，我还发了朋友圈。我跟朋友一直把他讲得挺体面，也没想承认自己其实很吃那种体面，怕突然承认他失业欠账，像是我自己看走眼。

新：

> 还有一句我没好意思说：他提过如果我这次不帮，以后他可能就不敢跟我谈结婚了，说自己会一直觉得低我一头。我前面也没说全，那周我们刚吃过很贵的纪念日晚餐，我还发了朋友圈。我跟朋友一直把他讲得挺体面，也没想承认自己其实很吃那种体面。真说出来，我也怕别人觉得我找了个撑不住场面的人，像是我自己看走眼。

### 修改 2：`sceneVersions[4].questionOptions[1].answer` 去重改写（外围，问句不动）

旧：

> 其实我是怕背这笔债，但又说不出口。我怕一拒绝就被说嫌贫爱富，我也怕别人觉得我找了个撑不住场面的人。前面我把他夸得体面，现在突然承认他失业欠账，我自己也挂不住。所以我才一直说要看账单。

新：

> 其实我是怕背这笔债，但又说不出口。我怕一拒绝就被说嫌贫爱富。刚才那句都说出口了……前面我把他夸得那么体面，现在改口，我自己也挂不住。所以我才一直说要看账单，先拖着。

### 修改 3：`quotePickCandidates` 与出货同步

新值：

```json
["我只是怕你知道我失业后就离开我", "我也怕别人觉得我找了个撑不住场面的人", "账单其实还有三天才到期", "以后他可能就不敢跟我谈结婚了"]
```

### 修改 4：`runtimeLengthPlan.truthBoundaryPromptCount` 改为 `5`（与 manifest tier1 限制一致）

---

## 案 2（02-tony.json）

走向表（不变部分）：V0 排他话术 → V1 情绪求助接转化 → V2 表格备注 → V3 老板娘话术 → V4 推进列。材料板 2 块，回流 1 条，均不动。

### 修改 1：`sceneVersions[1].version` 整段替换

旧：

> 后来我翻聊天，发现一个节奏特别明显。他先说“店里压力大”“今晚又被店长说了”，我一心软，他就接让我帮忙发活动、带朋友去剪头，或者问我下次要不要直接办年卡。

新：

> 后来我翻聊天，发现一个节奏特别明显。他先说“店里压力大”“今晚又被店长说了”，还说这些只跟我讲。我跟朋友复述的时候都说：“他说我像店里自己人。”我一心软，他就接让我帮忙发活动、带朋友去剪头，或者问我下次要不要直接办年卡。

说明：V0 外围回答里已有「他说我像店里自己人，我听着其实挺受用」，与新正文构成重复锚定（同一说法跨场景复现），符合语言规则，不需要改。

### 修改 2：`quotePickCandidates` 与出货同步

新值：

```json
["只有我能接住 TA 的情绪", "他说我像店里自己人", "我从来没说只有你一个", "以后店开起来，你就是老板娘"]
```

### 修改 3：`runtimeLengthPlan.truthBoundaryPromptCount` 改为 `5`（若该字段存在且不等于 5）

---

## 案 3（03-profile.json）

走向表（不变部分）：V0 见家长前查材料 → V1 介绍人抬版本 → V2 名校毕业实为 MBA + 来电人对家里的剪辑 → V3 花销对不上 → V4 工资卡反问。材料板 2 块，回流 1 条，均不动。

### 修改 1：`sceneVersions[2].version` 整段替换

旧：

> 后来我细问才发现，他说的名校毕业不是全假，但具体是读过一个名校 MBA 项目。他之前本科很普通，这段他一开始没主动说。

新：

> 后来我细问才发现，他一直说名校毕业，细问才说是 MBA，具体是读过一个名校 MBA 项目。他之前本科很普通，这段他一开始没主动说。我只说他学校那边确实是真的，本科的事，我没提。

### 修改 2：`sceneVersions[2].questionOptions[1]` 外围问答改写（正文已交代"没提"，原问句「跟家里说了吗」失效，改问为什么）

问句，旧：

> 你知道本科之后，跟家里说了吗？

问句，新：

> 本科这件事，你后来跟家里提过吗？

回答，旧：

> 没有。我只说他学校那边确实是真的。因为我前面已经把“名校毕业”讲得很好听了，再补一句本科很普通，我怕我妈立刻变脸。

回答，新：

> 提不了啊。“名校毕业”已经讲出去了，再补一句本科很普通，我妈立刻就得变脸。我想着流水那关要是过了，这事就当没发生过。

（routeAxis / routeTone / correct 三个字段不动。）

### 修改 3：`quotePickCandidates` 与出货同步

新值：

```json
["他一直说名校毕业，细问才说是 MBA", "我只说他学校那边确实是真的", "再问下去，是不是工资卡也要交出来？", "结婚以后钱最好放一起管"]
```

### 修改 4：`runtimeLengthPlan.truthBoundaryPromptCount` 改为 `6`（与 manifest tier3 限制一致，若不符）

---

## 案 4（04-workplace.json）

走向表（不变部分）：V0 主责自认+垫款 → V1 别在大群问预算 → V2 审批图缺半张 → V3 服务协调费与返款 → V4 署名与入口分离。材料板 2 块，回流 1 条，均不动。

### 修改 1：`sceneVersions[0].version` 整段替换

旧：

> 我一开始也不是完全被迫。我刚进项目组，确实想借这次客户答谢会让老板看到我。更难听一点，我先跟老板说过这次我可以主责，所以听到“署名写你负责”，就先垫了。

新：

> 我一开始也不是完全被迫。我刚进项目组，确实想借这次客户答谢会让老板看到我。更难听一点，我也确实想要这个主责，先跟老板说过这次我可以接。所以听到“署名写你负责”，就先垫了。

### 修改 2：`sceneVersions[0].questionOptions[1]` 去审问腔并去重改写（外围）

问句，新：

> 他说署名写你负责的时候，你当时怎么想的？

旧：

> 我也确实想要这个主责，所以没有第一时间追垫款流程。可想表现不等于默认钱可以一直卡着。

新：

> 我接了。说难听点，我就是想要这个主责。所以垫款流程我没第一时间追，怕一追就显得我斤斤计较。可想表现不等于钱可以一直卡在他手里。

说明：deepFollowup 里那句「我也确实想要这个主责」保留原样——正文先说过之后，深问里再出现就是回扣，不再是提前泄露。

### 修改 3：`quotePickCandidates` 与出货同步

新值：

```json
["报销审批通过", "我也确实想要这个主责", "返款统一打给对接人", "先私下把事办成，复盘再补流程"]
```

### 修改 4：`runtimeLengthPlan.truthBoundaryPromptCount` 改为 `6`（与 manifest tier4 限制一致，若不符）

---

## 实施顺序与验收

1. 按上文逐案修改四个 case JSON；若 `src/caseEngine.js` 里仍有同 plot 兜底模板，也要同步同一条链路。
2. `npm run content:index` 重新生成 `src/generated/contentPackIndex.js`。
3. `npm run check`；`npm run verify:pack -- steam-demo-01`。
4. `npm run smoke:browser`——若回放脚本断言了被改动的台词文本，同步更新脚本内的期望文本，不得反过来改台词。
5. `rg "撑不住场面|店里自己人|学校那边确实是真的|确实想要这个主责" content/ src/generated/` 确认每条在对应 `version` 正文中出现。
6. 把开头的引语↔出处账本表粘贴进 `content/packs/steam-demo-01/qa-report.md`。
7. 提交信息建议：`fix: unground gated final quotes into scene versions`。

## 不要做的事

- 不调整场景数量、选项数量、routeAxis/routeTone、材料板与回流结构。
- 不顺手润色未列出的台词；但若旧问法违反“真实连线口吻 / 禁止审问腔”，可以在保留原功能和无门控出处的前提下改成当前 skill 认可的主持人口吻。
- 不把 `quotePickCandidates` 反向改回旧候选——以出货 `accusationChoices` 为准。
