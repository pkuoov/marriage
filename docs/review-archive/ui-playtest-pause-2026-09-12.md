# 试玩暂停：首屏旁白与文字比例回退

状态：首次暂停时只记录。随后用户授权“UI 名牌和框体还是很奇怪。修改完之后再启动试玩”，现已修复并补验。地址为 http://127.0.0.1:5195/ 。测试使用独立浏览器上下文，未清除用户存档。

用户原话：

> 可以暂停了，第一页旁白的UI位置就不对，对话框内的文字在框中的比例整个都不对。UI全面倒退

按阻断继续试玩的问题记录。全局影响范围尚待逐屏确认，不能用首屏复现推定每个界面都相同。

## 已复现

- 首屏“旁白”呈现为框内居中的大名牌，正文却靠左、位于名牌下方；两者空间关系割裂。
- 1280×720：文本框约1120×181，正文23px，一行短旁白配宽框及较大上下空白。
- 592×920：文本框约574×152，正文降至16px；“旁白”名牌依旧显眼，底部设置按钮分成两行，视觉重量超过旁白。
- 时间、序章标题、地点说明和文本框叠在场景下半部；需要整体重审标题、名牌、正文、留白与工具栏的位置和比例。

截图：[1280宽](../../output/ui-regression-2026-09-12/first-page-1280.png)、[592宽](../../output/ui-regression-2026-09-12/first-page-592.png)。测量：[首屏尺寸](../../output/ui-regression-2026-09-12/first-page-metrics.json)。

## 定位线索

`src/styles/70-dialogue-stage.css` 的共用规则设置文本框最小高度、较大padding、旁白名牌居中与正文最小高度；`90-testimony-and-cafe-overrides.css` 和 `95-visual-polish.css` 继续覆盖咖啡厅与窄屏舞台。当前失衡可复现于这组最终样式，但尚未做版本对比，不能断言由哪一次提交引入。

## 验收缺口

上一轮台词与存档矩阵检查内容和操作，截图只检查收麦页遮挡，没有覆盖首页旁白的构图与比例。功能测试通过、元素不重叠，不等于视觉布局合格；之前这些结果不能作为整体UI验收结论。

恢复修改时先对照此前认可的视觉基线，检查首屏旁白、普通对白、多人对话、回放、追问、材料、收麦和片尾的共同容器；修复共享样式及覆盖关系后按真实窗口尺寸复看。不要先给第一页再叠一个局部补丁，也不在本次只记录阶段实施该方案。

## 本次修复与补验

- 单句名牌统一靠左贴框，不再按角色居中或右对齐；多句同屏保留各自名牌，滚动回放名牌放在裁切边界以内。
- 取消正文的额外最小高度和垂直居中，统一框内留白。窄屏正文恢复19px，桌面最大23px。
- 找到实际字号覆盖原因：`50-cafe-and-case-flow.css` 的移动端 `.vn-stage .dialogue-card p` 优先级高于AVG正文规则。将旧段落规则排除 `.avg-line`，不再继续增加字号覆盖层。
- 收拢咖啡厅标题，设置栏维持一行；修复窄屏立绘容器负边距导致人物脸部裁切。
- 在现有浏览器脚本加入 `--target=dialogue-layout`，检查完整首段六位发言者的文字尺寸、名牌与正文对齐、框体边界、工具栏遮挡和行数。四尺寸为390×844、592×920、1280×720、1280×800；截图见 `output/playwright/dialogue-layout/`。
- `npm run check` 通过；独立快案四尺寸流程通过，记录为 `output/playwright/nameplate-check.log`、`nameplate-quick-smoke.log`。排版回归记录为 `nameplate-layout-smoke.log`。已构建 playable。

### 尚未覆盖

本次不是全游戏视觉验收。原 `cafe-prologue` 完整测试含过期剧情断言：修正一处固定旧回应后，仍在旧法律台词断言处失败，后续还残留已取消的二选一支线预期。这条完整测试不能报告为通过，也没有据此恢复旧台词和旧分支。当前UI补验使用独立的排版目标，后续应按现行顺序流程迁移这条旧测试。原失败日志保留在 `output/playwright/nameplate-cafe-smoke.log`。
