# 9 月 12 日试玩反馈修复

状态：本轮十项反馈（含此前已改的一处措辞）已接入。定向浏览器复验通过；回归结果见下方。

| 反馈 | 修复 |
| --- | --- |
| 退出记录后，逐句追问的主对话框移到屏幕外 | 回放改用网格行后清除遗留的 `translateX(-50%)` 和固定宽度；对话、选项各占一行。案卷打开、切换焦点时禁止隐式滚动。 |
| 妻子铭牌被遮挡 | 咖啡厅对白容器允许边缘铭牌完整显示，取消包住铭牌的滚动裁切。 |
| “出示：澜桥酒店”内容很少却有滚动条、遮挡 | 取消材料选择区继承的最大高度和嵌套滚动，保留材料板及出示按钮的自然高度；原件仍通过独立弹窗查看。 |
| “先听她说完”位置不对 | 舞台状态标签移到顶部装饰条下方，避免被覆盖。 |
| 周会计不能直接拉进电话 | 改为创建临时微信群并发起群语音；接通后的男方标识和周会计口吻同步改成微信语音。 |
| 第一案前缺少“两年前”转场 | 咖啡厅标为现在；家庭卡交接后出现独立“两年前”页面，点击进入 2024 年 7 月 15 日的直播。四案回忆结束后，回到咖啡厅之后数周的回告。旧存档已开始直播时不会被倒推回转场。 |
| 老方不在场却知道工牌压线 | 交代老方站在桌边，身份标为“运营 · 老方（现场）”；主播当面回应后，交代老方离开。 |
| 老婆确实发了消息，应写明微信 | 保留消息类型和赵律师角色绑定，显示“老婆的微信”。 |
| “她问钱花去哪儿”不顺 | 保留已修改的“她问钱都花哪去了”。 |
| 社保记录突然拿出，缺少原因 | 补齐“主播索要他发来的记录 → 来电人转给后台 → 后台收到 → 继续核问”，不扩大社保记录能证明的事实。 |

## 验证

- 咖啡厅完整流程：390×844、592×920、1280×720、1280×800 四种视口通过，覆盖酒店材料、两项顺序调查、转场、回告和存档。
- 定向回归：1280×800、390×844 通过，检查回放及关闭案卷后的对话框边界、状态标签可见、延后扣耐心、社保追问、账单退出和微信角色。
- 人工浏览器检查：妻子铭牌完整；酒店出示页容器的 `scrollHeight` 与 `clientHeight` 相等，无嵌套滚动；逐句追问和记录关闭后的对话完整可见。
- `npm run check`、`npm run smoke:desktop` 通过。故事维护报告的结构、链接、连续性检查均无错误（另有两项既存警告）。
- 阅读稿与运行时索引已同步；剧情文档已明确现在／两年前的时间层。
- 全量 `npm run smoke:browser` 尚未通过：第一案第二夜触发 `night-B counter-pressure should interrupt between two live scenes` 台词断言。本轮未修改该段第二夜台词；不能把本次定向验收记成全路线通过。复现输出见本机 `/tmp/love-ui-browser.log`。
- 最终复核：运行时索引、生成稿一致性、129 项逻辑测试、桌面构建及 `git diff --check` 通过。额外的 `dialogue-layout`／`script-reading` 浏览器验证未启动：自动审批因服务连接中断而拒绝执行；此前通过的定向浏览器结果保留。

自动验证不替代下一轮真人连续试玩。旧对话记录保留当时读到的内容；新台词以当前剧情真源为准。
